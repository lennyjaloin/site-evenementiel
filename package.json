{
  "name": "site-evenementiel",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend_complete/web install && npm --prefix frontend_complete/web run build",
    "dev:web": "npm --prefix frontend_complete/web run dev",
    "build:web": "npm --prefix frontend_complete/web run build",
    "preview:web": "npm --prefix frontend_complete/web run preview",
    "dev:mobile": "start_mobile.bat",
    "mobile:android": "npm --prefix frontend_complete/mobile run android",
    "mobile:ios": "npm --prefix frontend_complete/mobile run ios",
    "dev:backend": "npm --prefix backend_complete run dev",
    "start:backend": "npm --prefix backend_complete run start"
  }
}
